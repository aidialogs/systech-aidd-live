---
alwaysApply: true
---
# Правила разработки кода

> Полное техническое видение проекта: @vision.md

## Главные принципы

### KISS - Keep It Simple, Stupid
- Никакого оверинжиниринга
- Только необходимое для работы MVP
- Простой и понятный код
- Минимум слоев и абстракций

### Организация кода
- **Один класс = один файл** - строгое правило
- **Плоская структура** - все в `src/` без вложенных пакетов
- **Минимум файлов** - только необходимое

### Асинхронность
- Весь код асинхронный (async/await)
- aiogram и openai используют asyncio

## Что НЕ делать

### ❌ Запрещено использовать:
- Type hints и аннотации типов
- Линтеры и форматтеры в коде
- Сложные паттерны (фабрики, строители, стратегии)
- DI-контейнеры и IoC
- Избыточную абстракцию и многоуровневую иерархию
- Dataclasses, Pydantic models
- Базы данных (только in-memory)
- Docker, CI/CD

## Стиль кода

### Форматирование
- Базовый PEP 8 (визуально)
- 4 пробела для отступов
- Читаемые имена переменных

### Структура классов
- Классы - для компонентов и структур данных
- Функции - для простой обработки
- Простой `__init__` с явными параметрами

### Обработка ошибок
- Простой try/except
- Логирование всех ошибок
- Дружелюбные сообщения пользователю

## Зависимости

См. полный список в @vision.md (раздел 1)

Основные:
- Python 3.11+
- aiogram 3.x
- openai
- python-dotenv

## Структура файлов

См. полную структуру в @vision.md (раздел 3)

```
src/
├── main.py              # Точка входа
├── config.py            # Конфигурация
├── message.py           # Message класс
├── message_handler.py   # MessageHandler класс
├── llm_client.py        # LLMClient класс
└── context_manager.py   # ContextManager класс
```

## Проверка перед коммитом

- [ ] Один класс = один файл
- [ ] Нет type hints
- [ ] Нет сложных паттернов
- [ ] Код асинхронный (async/await)
- [ ] Плоская структура (всё в src/)
- [ ] Логирование ошибок
- [ ] Базовый PEP 8 визуально

