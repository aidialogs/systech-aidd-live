# Итерационный план разработки

> Техническое видение: @vision.md  
> Правила разработки: @conventions.md

---

## 📊 Отчет по прогрессу

### MVP (базовый функционал)
| Итерация | Задача | Статус | Дата |
|----------|--------|--------|------|
| 0️⃣ | Эхо-бот | ✅ Завершено | 2025-10-10 |
| 1️⃣ | Интеграция LLM | ✅ Завершено | 2025-10-10 |
| 2️⃣ | Интеграция истории диалога | ✅ Завершено | 2025-10-10 |
| 3️⃣ | Команды и обрезка истории | ✅ Завершено | 2025-10-10 |
| 4️⃣ | Финальное тестирование | ✅ Завершено | 2025-10-10 |

### Технический долг
| Итерация | Задача | Статус | Дата |
|----------|--------|--------|------|
| 5️⃣ | Обновление документации | ✅ Завершено | 2025-10-11 |
| 6️⃣ | Инструменты качества кода | ⏳ Ожидает | - |
| 7️⃣ | Type hints и Mypy | ⏳ Ожидает | - |
| 8️⃣ | Рефакторинг архитектуры | ⏳ Ожидает | - |
| 9️⃣ | Покрытие тестами 80%+ | ⏳ Ожидает | - |

**Легенда статусов:**
- ⏳ Ожидает
- 🚧 В работе
- ✅ Завершено
- ⚠️ Требует доработки

---

## 🚀 Итерации разработки

### Итерация 0: Эхо-бот

**Цель:** Подготовить окружение, выстроить правильную архитектуру с MessageHandler-заглушкой

**Инициализация:**
- [x] Создать структуру директорий (src/, tests/, logs/, doc/)
- [x] Создать `pyproject.toml` с зависимостями (aiogram, openai, python-dotenv)
- [x] Создать `.env.example`
- [x] Создать `.gitignore`
- [x] Создать `Makefile` с командами
- [x] Создать `README.md`
- [x] Выполнить `make install`

**Файлы:**
- [x] `src/__init__.py` (пустой)
- [x] `src/config.py` - класс Config с загрузкой .env (BOT_TOKEN, LLM_*)
- [x] `src/message_handler.py` - класс MessageHandler (пока заглушка)
  - `__init__()` - пустой конструктор
  - `async handle_message(message, user_id, chat_id)` - возвращает "Вы написали: {текст}"
- [x] `src/main.py` - инициализация aiogram + MessageHandler, подключение к dispatcher

**Логирование:**
- [x] Настроить базовый logging в `main.py` (файл `logs/app.log` + консоль)
- [x] Логировать запуск бота: "Bot started"
- [x] Логировать остановку бота: "Bot stopped"

**Архитектура:**
```
Telegram → MessageHandler (заглушка) → Echo ответ
```

**Функционал:**
- Загрузка конфигурации из .env
- Запуск бота через `make run`
- MessageHandler координирует обработку (пока просто эхо)
- Ответ на любое текстовое сообщение: "Вы написали: {текст}"
- Базовое логирование lifecycle событий

**Тест:** 
- Написать боту в Telegram "Привет" → получить "Вы написали: Привет"
- Проверить логи → "Bot started" присутствует в `logs/app.log`

---

### Итерация 1: Интеграция LLM

**Цель:** Подключить LLM API, отвечать без истории диалога

**Файлы:**
- [x] `src/message.py` - класс Message (role, content, to_dict)
- [x] `src/llm_client.py` - класс LLMClient
  - `__init__(api_key, base_url, model)`
  - `async get_response(messages)` - возвращает текст ответа

**Доработка MessageHandler:**
- [x] Добавить в `__init__`: llm_client, system_prompt
- [x] Обновить `handle_message`: формировать [system_prompt, user_message] → LLM → ответ

**Интеграция в main.py:**
- [x] Создать экземпляр LLMClient
- [x] Передать LLMClient в MessageHandler
- [x] MessageHandler использует LLM вместо эхо

**Логирование:**
- [x] Логировать входящие сообщения: user_id, chat_id, текст
- [x] Логировать LLM запросы: модель, количество сообщений
- [x] Логировать LLM ответы: длительность, статус (success/error)
- [x] Логировать ошибки LLM API с деталями

**Архитектура:**
```
Telegram → MessageHandler → LLMClient → LLM API
```

**Функционал:**
- Отправка разовых запросов к LLM
- Каждый запрос независимый (без памяти)
- Конвертация Message в формат OpenAI
- Базовая обработка ошибок (try/except)
- Логирование всех LLM взаимодействий

**Тест:** 
- Unit-тест `tests/test_llm_client.py` → запрос к реальному API возвращает ответ
- Написать боту вопрос → получить ответ от LLM (без учета предыдущих сообщений)
- Проверить логи → есть записи о LLM запросе/ответе с длительностью

---

### Итерация 2: Интеграция истории диалога

**Цель:** Добавить память — бот помнит контекст разговора

**Файлы:**
- [x] `src/context_manager.py` - класс ContextManager
  - `add_message(user_id, chat_id, message)`
  - `get_context(user_id, chat_id)`
  - `clear_context(user_id, chat_id)`

**Доработка MessageHandler:**
- [x] Добавить в `__init__`: context_manager
- [x] Обновить `handle_message`: 
  - Получить контекст из ContextManager
  - Добавить user message в контекст
  - Передать всю историю в LLM
  - Добавить assistant message в контекст

**Интеграция в main.py:**
- [x] Создать экземпляр ContextManager
- [x] Передать ContextManager в MessageHandler

**Логирование:**
- [x] Логировать создание нового контекста для пользователя
- [x] Логировать добавление сообщений в контекст
- [x] Логировать размер контекста при каждом запросе

**Архитектура:**
```
Telegram → MessageHandler ← ContextManager (in-memory dict)
               ↓
           LLMClient → LLM API
```

**Функционал:**
- Хранение истории диалогов в памяти (dict)
- Получение полной истории по (user_id, chat_id)
- Добавление user/assistant сообщений в контекст
- LLM видит всю историю диалога
- Логирование операций с контекстом

**Тест:** 
- Unit-тест `tests/test_context_manager.py` → добавление/получение работает
- Написать боту: "Меня зовут Иван" → потом "Как меня зовут?" → бот помнит имя
- Проверить логи → есть записи о создании контекста и размере истории

---

### Итерация 3: Команды и обрезка истории

**Цель:** Команды бота, ограничение контекста, логирование, обработка ошибок

**Доработка:**
- [x] Добавить команды в MessageHandler:
  - `/start` - приветствие
  - `/help` - справка
  - `/reset` - очистка контекста
- [x] Реализовать обрезку истории (max_context_messages в ContextManager)
- [x] Обернуть LLM запросы в try/except с дружелюбными сообщениями

**Логирование:**
- [x] Логировать выполнение команд (/start, /help, /reset)
- [x] Логировать очистку контекста
- [x] Логировать обрезку истории (когда удаляются старые сообщения)
- [x] Улучшить формат логов согласно @vision.md (раздел 10)

**Функционал:**
- `/start` → "Привет! Я AI-ассистент. Используй /help для справки"
- `/help` → Список команд с описанием
- `/reset` → "История диалога очищена. Начнем сначала!"
- Ограничение контекста (20 сообщений) с автоматическим удалением старых
- System prompt всегда сохраняется
- Логи в `logs/app.log` + консоль
- При ошибке LLM → дружелюбное сообщение
- Полное логирование всех операций

**Тест:**
1. `/start` → приветствие + в логах запись о команде
2. `/help` → список команд + в логах запись
3. Диалог → `/reset` → контекст очищен + в логах запись об очистке
4. Длинный диалог (>20 сообщений) → старые удаляются + в логах запись об обрезке
5. Симуляция ошибки API → дружелюбное сообщение + в логах ERROR
6. Проверить формат логов согласно @vision.md

---

### Итерация 4: Финальное тестирование

**Цель:** Проверка всех сценариев работы

**Проверка:**
- [x] Запуск через `make run`
- [x] Команда `/start` работает
- [x] Команда `/help` работает
- [x] Команда `/reset` работает
- [x] Обычный диалог работает
- [x] Длинный диалог (>20 сообщений) обрезает контекст
- [x] Ошибки LLM обрабатываются корректно
- [x] Логи пишутся в файл
- [x] Unit-тесты проходят (`make test`)

**Тест:** Все сценарии из @vision.md (раздел 8) работают корректно

---

## 🔧 Технический долг

| Итерация | Задача | Статус | Дата |
|----------|--------|--------|------|
| 5️⃣ | Обновление документации | ✅ Завершено | 2025-10-11 |
| 6️⃣ | Инструменты качества кода | ⏳ Ожидает | - |
| 7️⃣ | Type hints и Mypy | ⏳ Ожидает | - |
| 8️⃣ | Рефакторинг архитектуры | ⏳ Ожидает | - |
| 9️⃣ | Покрытие тестами 80%+ | ⏳ Ожидает | - |

---

### Итерация 5: Обновление документации

**Цель:** Привести документацию в соответствие с новыми практиками

**Правки в `doc/vision.md`:**
- [x] Раздел 2, строки 44-45: удалить запреты на type hints и линтеры/форматтеры
- [x] Раздел 5, строка 203: удалить запрет на dataclasses
- [x] Раздел 9, строка 425: удалить запрет на dataclasses для конфига

**Правки в `.cursor/rules/conventions.mdc`:**
- [x] Строки 28-29, 33: удалить из "❌ Запрещено" - type hints, линтеры, dataclasses
- [x] Строка 81: удалить из чеклиста "Нет type hints"
- [x] Добавить новые правила качества кода (type hints обязательны, make quality)

**Тест:**
- Документация читается без противоречий
- Правила соответствуют плану технического долга

---

### Итерация 6: Инструменты качества кода

**Цель:** Автоматизация проверки стиля и качества

**Установка:**
- [ ] Добавить в `pyproject.toml`: ruff, mypy, pytest-cov
- [ ] Настроить `[tool.ruff]` (line-length=100, select правила)
- [ ] Настроить `[tool.pytest.ini_options]` (asyncio_mode="auto")
- [ ] Настроить `[tool.coverage.report]` (fail_under=80)
- [ ] Обновить Makefile: `lint`, `format`, `type-check`, `test-coverage`, `quality`

**Тест:**
- `make format` → код отформатирован
- `make lint` → нет ошибок стиля
- `make quality` → все проверки проходят

---

### Итерация 7: Type hints и Mypy

**Цель:** Строгая типизация для maintainability

**Доработка:**
- [ ] Добавить type hints во все классы (`config.py`, `message.py`)
- [ ] Добавить type hints в `llm_client.py` (AsyncOpenAI, return types)
- [ ] Добавить type hints в `context_manager.py` (dict типы, List[Message])
- [ ] Добавить type hints в `message_handler.py` (aiogram types)
- [ ] Настроить `[tool.mypy]` (strict=true, disallow_untyped_defs=true)

**Тест:**
- `make type-check` → 0 ошибок mypy
- `make quality` → все проверки проходят

---

### Итерация 8: Рефакторинг архитектуры

**Цель:** SRP, улучшенная конфигурация

**Рефакторинг:**
- [ ] `config.py` → переписать через `@dataclass`, добавить `__post_init__` валидацию, `from_env()`
- [ ] `src/command_handler.py` → выделить обработку команд из MessageHandler
- [ ] `message_handler.py` → упростить, делегировать команды в CommandHandler
- [ ] `main.py` → использовать `Config.from_env()`, инициализация CommandHandler

**Тест:**
- Все существующие тесты проходят
- Запуск `make run` работает
- Команды `/start`, `/help`, `/reset` работают

---

### Итерация 9: Покрытие тестами 80%+

**Цель:** Надежность через тестирование

**Файлы:**
- [ ] `tests/conftest.py` → fixtures (context_manager, mock_llm_client)
- [ ] `tests/test_message_handler.py` → тесты команд, обработка ошибок
- [ ] `tests/test_llm_client.py` → mock OpenAI, error handling
- [ ] Дополнить `test_context_manager.py` → edge cases, concurrent access
- [ ] Дополнить `test_integration.py` → полный flow диалога

**Тест:**
- `make test-coverage` → coverage ≥ 80%
- Все тесты проходят
- HTML отчет в `htmlcov/index.html`

---

## 📝 Примечания

- Каждая итерация независимо тестируется
- Можно остановиться на любом этапе - бот работает
- Следуем принципу KISS - никаких лишних усложнений
- Один класс = один файл (строго)
- Весь код асинхронный (async/await)

**После завершения итерации:**
1. Обновить таблицу прогресса
2. Зафиксировать дату
3. Отметить чекбоксы

